import {
  View,
  Text,
  ScrollView,
  TextInput,
  TouchableOpacity,
} from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const QR = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>QR</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default QR;
